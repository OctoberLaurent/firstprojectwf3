<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}{{ encore_entry_link_tags('app') }}{% endblock %}
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous">
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="col-3">
                {% block sidebar %}
                <ul>
                    <li>
                        <a class="nav-link" href="{{ url('home_page') }}">Accueil <span class="sr-only">(current)</span></a>
                    </li>
                    <li>
                        <a class="nav-link" href="{{ url('books:index') }}">Livres</a> 
                    </li>
                </ul>
                {% endblock %}
                </div>
                <div class="col-9">
                    {% block content %} {% endblock %}
                </div>
            </div>
        </div>

        <nav>
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        {# Authenticated user #}
            <a href="{{ path('books:index') }}">user name</a>
            <a href="{{ path('app_logout') }}">Deconnexion</a>
        {% else %}
        {# Anonymous user #}
            <a href="{{ path('register') }}">Inscription</a>
            <a href="{{ path('app_login') }}">Connexion</a>
        {% endif %}
        </nav>

        {% block body %}{% endblock %}
        <footer>
        <div class="container">
        <div class = "text-center">{{ copyright(2017) }}
        <copyright>
        </div>
        </div>
        </footer>
        {% block javascripts %}{{ encore_entry_link_tags('app') }}{% endblock %}
        
    </body>
</html>
